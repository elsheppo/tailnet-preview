{
  "name": "tailnet-preview",
  "private": true,
  "version": "0.1.0",
  "description": "Private, durable web previews for trusted Tailscale devices",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.mjs",
    "check": "npm run build && npm test"
  }
}
